# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Large data/model files
data/ml_dataset.json
models/report_dqn_*.json
data/*.db
data/*.db-shm
data/*.db-wal
scripts/python/venv/

# SQLite database
ict-trading.db

# Worktrees
.worktrees/

# Portfolio allocator + decay monitor runtime outputs (path A)
data/allocator-recommendations.json
data/decay-status.json
data/decay-alerts-log.json
data/book-governance.json

# 1m candle datasets (100-500MB each — regenerate via scripts/download-gold-1m.ts etc.)
data/*_1m.json
data/*_1m_holdout.json
data/*_1h_holdout.json
data/.duka-1m-chunks-*/
data/.gold-1m-chunks/
# Runtime state/streams
data/orderflow/
data/metals-bot-state.json
data/gold-bot-state.json
logs/

# Regenerated Pine port artifact (written by a local Stop hook; not branch source)
pine/run20-confluence-ob.pine

# Local agent state (Claude Code, Ralph loop) — never commit
.claude/
.superpowers/
data/*.db.bak*
simulation-report.html
experiments/runs/allocator-report.json
# Research-loop momentum combine artifacts (regenerable via scripts/add-momentum-sleeve.ts)
experiments/runs/strategy-daily-returns-plus-momentum.json
experiments/runs/combined-daily-E.json
experiments/runs/combined-daily-F.json
data/letf-bot-state.json
